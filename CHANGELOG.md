# 更新日志

项目遵循面向用户的版本记录。每个发行版同时发布 Windows 和 macOS 的可下载资产，并在 Release 页面记录校验值和已知限制。

## [Unreleased]

## [v2.2.0] - 2026-09-20

- **自适应布局**：新增三档宽度断点（≤1100 / ≤820 / ≤560px），窗口缩放时侧边栏、表单栅格、结果卡片与看板自动重排；侧边栏在窄屏收为图标条，鼠标悬停显示名称。
- **自适应布局**：看板弹窗增加 `max-width: 92vw`，窄窗口下不再溢出屏幕。
- **动效增强**：统计数字改为平滑滚动递增（公网与内网共 9 个指标），避免结果瞬间跳变。
- **动效增强**：日志行滑入、看板图表错峰入场、弹窗与灯箱遮罩淡入、结果卡片搜索或排序后错峰重排。
- **交互反馈**：新增轻量 toast，用于复制 IP、导出图表等操作的即时提示，1.9 秒后自动淡出。
- **交互反馈**：为导航项与各类按钮补充键盘 `:focus-visible` 焦点描边，Tab 操作可辨识。
- **界面精简**：移除左上角品牌区、左下角状态灯、各页冗余副标题与侧边栏分组标签；扫描配置与子域发现合并为同一张卡片，子域发现默认折叠。
- **界面精简**：移除「最快路径」与「关于子域发现」两段说明性文字；图表「导出 PNG」按钮改为悬停或聚焦时才浮现，减少常驻视觉噪音。
- **关于页**：新增运行环境信息（版本、运行平台、Python 版本、首页截图可用性、结果目录），并支持一键在系统文件管理器中打开结果目录。
- **修复**：摘要条在扫描未开始时显示「目标 undefined 个」——数值读取增加回退逻辑，并在初始化时预置统计值。
- **修复**：Chromium 路径检测漏掉新版 `chrome-mac-x64` 与 `Google Chrome for Testing.app`，导致环境明明可用却误报「未找到 Chromium」、截图功能被关闭。
- **修复**：已知子域验证误把 301 跳转页与通用占位页判为活靶场——域名目标改走 `probe_domain`，跟随重定向并过滤通用标题。
- **工程整理**：浏览器预览脚本移入 `scripts/`，修正移动后模块导入路径失效导致的 502。
- **子域发现提速（约 3 倍）**：整轮只解析一次通配符后端 IP（原本每候选解析 1~2 次 DNS，单次约 130ms）；引入按线程复用的 HTTP 长连接池，省掉每候选的 TCP+TLS 握手（约 415ms）；新增端口校准，发现平台把 80 统一 301 到 443 后整轮直连 443，省去跳转往返。
- **子域发现并发收敛**：实测吞吐随并发先升后降（20 并发 107/s、50 并发 117/s、200 并发反降至 79/s，延迟从 165ms 升至 508ms），说明服务端存在限速。因此新增并发上限（64），快速模式不再盲目拉高线程——此前默认的 300~400 线程实际更慢。
- **子域发现字符集**：新增字符集选择（字母+数字 / 仅小写 / 十六进制 / 仅数字 / 自定义），并实时显示组合空间与预计耗时。穷举 36^6≈21.8 亿在限速下需数百天，缩小字符集是让发现变可行的唯一手段（十六进制 16^6≈1677 万，约数十小时）。
- **修复**：子域发现显式传入 `charset=None` 会抛 `TypeError`（GUI 过去一直硬编码 `charset=None`，靠上游兜底才未暴露），已在入口统一回退到默认字符集。
- **可维护性**：将 24 处宽泛 `except Exception` 收窄为具体异常类型（DNS、网络、JSON、Playwright、子进程等），仅保留 4 处线程/程序边界的宽泛捕获并回传完整 traceback，避免出错时只看到 `repr(e)` 而无从排查。
- **已知子域轮询监控**：子域发现面板新增「监控已知子域」。靶场按需随机生成域名且拉起需要时间，穷举不可行时，对已知 ID 列表轮询等待的命中率高得多。支持一次填多个（逗号或换行，只填随机位会自动套用 `lab-??????.rzsec.cn`）、自定义轮询间隔（5–3600 秒）与端口、全部上线即停，列表实时显示每个域名的在线状态与页面标题。监控与扫描可同时进行，互不占用。
- **修复**：监控的事件回调辅助函数此前写在 `bindUI()` 内部，而事件处理器是顶层函数，作用域不可见会抛 `ReferenceError`；同时事件轮询的 `catch` 是空的，异常被完全吞掉，表现为「后端线程在跑、界面毫无反应」。已将辅助函数提到顶层，并让前 3 次轮询异常写进日志面板。
- **修复**：旧监控迟到的 `watch_done` 会关掉新监控的事件轮询定时器，导致界面不再刷新。现在每次监控带一个自增会话号，前端丢弃会话号对不上的过期事件。
- **修复**：点击「停止监控」后状态栏僵在「监控中」——原先立刻停掉轮询，后端收尾的统计事件再也没人取。改为等待收尾事件落终态（如「监控结束：2 轮，在线 0/2」），并加 3 秒兜底。
- **修复**：子域发现折叠面板用 `max-height` 硬编码（900px / 窄屏 1200px），新增内容后底部按钮会被裁掉且点不到。改用 `grid 0fr→1fr` 按内容真实高度展开，不再需要维护魔法值。
- **修复**：子域发现面板折叠时内容虽被裁剪，键盘 Tab 仍能聚焦到看不见的输入框。已补 `visibility: hidden` 移出焦点顺序与无障碍树，并延迟到收起动画播完再生效。
- **修复**：监控输入 `@@`、`-`、`lab_abc` 这类写法会被拼成 `lab-@@.rzsec.cn` 等无效域名混进监控列表，现已过一遍主机名校验并丢弃。
- **修复**：浏览器预览服务对返回布尔值的接口（`is_watching`）序列化失败，前端拿到空响应。
- **测试**：新增 `tests/test_watch.py`（15 条），覆盖域名规整、入参校验、事件流与会话隔离，全程用假探测函数、不发真实网络请求。全量用例 47 → 62。
- **文档**：README 首页预览图更新为新版界面截图；展示素材目录由 `docs/assets/` 统一为 `.github/assets/`，README、Windows 打包脚本与项目结构测试同步调整。

## [v2.1.2] - 2026-08-21

- Windows 默认发行包恢复公网首页截图：Playwright 协议运行时进入 EXE，Chromium 和 Node 驱动作为同级 `playwright-browsers/`、`playwright-runtime/` 资源随包提供。
- Windows 截图显式启动完整 Chromium 并绕过系统代理，兼容 `--no-shell` 发行资源，修复页面加载超时和截图回传失败。
- 公网结果统一过滤为 Ping 存活、页面可访问且标题非空的靶场页面，报告、历史看板和站点卡片保持一致。
- 增加 Windows 发行包组装脚本，统一 EXE、VBS、README、预览图和 Chromium 的文件边界。
- 增加项目文件管理规范，明确源码、文档、测试和 `.artifacts/` 生成物归属。
- 统一内网 CLI、GUI 和核心默认并发为 64。

## [v2.1.1] - 2026-08-21

- 公网扫描默认端口范围统一为 `8000–8020`。
- 修复 Windows EXE 的多尺寸 VLUN 图标嵌入，避免资源管理器回退显示通用图标。
- 增加 macOS Apple Silicon 和 Intel 应用包的自动构建与上传流程。
- 增加 README 首页预览图和文档素材目录。

## [v2.1.0] - 2026-08-21

- 整理运行代码、桌面资源、启动器、构建脚本和测试目录。
- Windows 默认构建精简版 EXE，不内置 Chromium。
- 统一 Windows/macOS 页面视觉和交互细节。
- 增加跨平台 CI、项目结构回归测试和 Windows 构建检查。

## [v2.0.0] - 2026-08-20

- 完成 macOS / Windows 双平台启动入口和平台适配。
- 提供 Windows 便携式桌面发行包。
- 公网 Web 与内网发现共用扫描核心，输出 HTML、Markdown、CSV、JSON 报告。

[Unreleased]: https://github.com/Chengxiaoyu1119/yucedu-vlunhub-scan/compare/v2.2.0...HEAD
[v2.2.0]: https://github.com/Chengxiaoyu1119/yucedu-vlunhub-scan/releases/tag/v2.2.0
[v2.1.2]: https://github.com/Chengxiaoyu1119/yucedu-vlunhub-scan/releases/tag/v2.1.2
[v2.1.1]: https://github.com/Chengxiaoyu1119/yucedu-vlunhub-scan/releases/tag/v2.1.1
[v2.1.0]: https://github.com/Chengxiaoyu1119/yucedu-vlunhub-scan/releases/tag/v2.1.0
[v2.0.0]: https://github.com/Chengxiaoyu1119/yucedu-vlunhub-scan/releases/tag/v2.0.0
